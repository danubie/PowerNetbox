*.TempPoint.*
*.Export.*
*.psprojs
*.psbuild
*.RestorePoint.*
*.Run.ps1
*.Package.ps1
CustomMenu.inf
Test-Module.ps1
Staging/
concatenated.ps1

# Local configuration (contains secrets)
.netboxps.config.ps1
.netboxps.config.example.ps1
*.local.ps1

# API keys and secrets
.psgallery.key
*.apikey

# Development files (keep in dev branch only)
.claude/*
!.claude/skills/
!.claude/skills/**
CLAUDE.md
Connect-DevNetbox.ps1
.vscode/

# Test VM configuration (contains tokens)
.netbox-test-vms.ps1

# Build output
PowerNetbox/
NetboxPSv4/

# Git worktrees (isolated workspaces for parallel branch work)
.worktrees/

# Test artifacts
coverage.xml
*.coverage.xml
TestResults/
*.trx

# Dev-only files (exclude from main branch merges)
# These files exist on dev but should NOT be merged to main:
# - Test-ExeDevVMs.ps1
# - scripts/setup-exe-dev-environment.sh
# - .github/workflows/exe-dev-tests.yml

# Internal planning and design artifacts (local-only)
# These document in-progress work for future-Claude context but are
# not intended for public consumption. docs/superpowers/reviews/ is
# deliberately excluded from this ignore — security reviews remain
# public as a community trust signal (see CLAUDE.md "Security & reviews").
docs/superpowers/plans/
docs/superpowers/specs/
